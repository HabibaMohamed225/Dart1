import 'dart:io';
List<int> fibonacci(int n) {
  List<int> list = [1, 1];
  for (int i=0;i<n;i++) {
    list.add(list[i] + list[i + 1]);
  }
  return list;
}
void main() {
  print("Enter the number: ");
  int number = int.parse(stdin.readLineSync()!);
  List<int> result = fibonacci(number);
  print(result);
}
